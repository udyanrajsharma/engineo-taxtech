create or replace PACKAGE XX_BVC_TEST_GSTR_TWO 
    AS
    
    
    PROCEDURE get_gstr_two_BIP_report_from_fusion;

    PROCEDURE get_presigned_url(gstr2_file_bytes IN CLOB);

    PROCEDURE upload_file_to_storage(presigned_url IN CLOB, file_name IN VARCHAR2, gstr2_file_bytes IN CLOB);

    PROCEDURE trigger_file_ingestion(presigned_url IN VARCHAR2, userfilename IN VARCHAR2);

    PROCEDURE get_file_ingestion_status(response IN CLOB);
    
--    FUNCTION decode_base64 (p_clob_in CLOB)
--      RETURN BLOB;
--      function DecodeToBlob(pIP clob) return blob;
--   function clobbase642blob( p_clob clob )
--return blob;

    END;